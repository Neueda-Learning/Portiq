# Used by docker-compose.yml to configure the local MySQL container.
# Copy this file to .env and adjust as needed: cp .env.example .env

DB_NAME=portiq_db
DB_USERNAME=portiq
DB_PASSWORD=portiq
DB_ROOT_PASSWORD=portiq_root
